import 'package:flutter/material.dart';

class Code {
  String code;
  num points;
  Code(this.code, this.points);
}

class ImageLinks {
  String link;
  ImageLinks(this.link);
}

class User {
  num userPoint;
  User(this.userPoint);
}
